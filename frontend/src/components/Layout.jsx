import { Outlet } from "react-router-dom";

/**
 * Represents the children of layout component.
 *
 * @returns {ReactElement} The rendered layout component.
 */
const Layout = () => {
    return (
       <Outlet/>
    )
}
export default Layout
